# 全面 UI 排布优化

用户在 `db3be6d` 修复后真机试用，要求全面优化 UI，并明确指出多个按钮文字贴顶。本轮从当前源码增量处理，不恢复历史 Next-Gen/C3。业务流程、动作语义和账本合同沿用当前实现。

## 执行顺序

1. 修复确定性排版缺陷：打水单行按钮垂直居中；资料选项触达；设置快捷选项换行与间距；赛事次级按钮缺失样式。
2. 用户随后明确本轮无需 Edge 对比，直接优化原生 UI；完成后启动自动真机调试。打水总账、记一局与发起页优先逐页落实。
3. 赛事操作链：大厅配置、录分、赛程、排名、复盘，优先操作层级、名称/比分对齐和底部操作区。
4. 首页、我的、资料、偏好、创建、反馈和分享入口，核验卡片密度、表单节奏、空态与错误态。

## 首批发现

- 打水统一重置 `line-height:1.2` 与 `padding:0`，部分 44–48px 操作按钮缺少垂直居中。明确单行控件补 flex 居中，复合流水行、球友网格和历史行保留自身布局。
- 设置页快捷选项使用仅在大厅页面定义的局部样式，需补本页排布。
- 资料性别选项 `80rpx` 在 320/390px 屏幕不足 44px。
- analytics/ranking/schedule 使用无定义的 `btn-outline`，需采用既有次级按钮样式。

## 验证与边界

- 首批打水回归先失败后通过，按钮及打水样式直接测试 30/30。
- 390px 总账当前原生图已通过 runner 与主控检查，加减按钮文字居中。首张证据：`tmp/ui-runs/simulator-frame/2026-09-12T15-34-51-180Z-74748-083a7a39/manifest.json`。
- 其他页面和弹层继续检查；此记录不表示全面 UI 已完成。最终原生图、双隔离评审和用户验收按 [视觉门禁](../tools/weapp-ui-acceptance.md)。用户本轮明确跳过浏览器对比。
- 本轮未执行提交、推送、客户端发布或新云部署；本地样式改动不自动出现在用户手机调试包。

## 本轮实现与复核（2026-09-13 凌晨）

- 已落实首批4项，并补上公共按钮对微信默认宽度/外边距的覆盖；发起页减少渐变/阴影、增加卡片留白、发起按钮靠右；录分/设置状态标签使用可布局的 view；排名战绩可自然换行；赛程使用有效文字色。
- 打水5字起使用长名布局，窄屏长名两列，长名1v1/2v2保留完整阵容展开入口。原生图发现网格按钮被微信默认宽度撑大，已对网格/侧别按钮明确100%宽度上限。
- 两名隔离评审共同指出名单覆盖水数选择框的P1，已通过滚动区域裁切及独立底部层级关闭。主控亲眼确认水数整行与确认按钮完整。
- 全量1486项：1480通过、6跳过、0失败（`tmp/ui-polish-final-full-test.log`）；此后局部样式/原生标签/截图状态约束变更直接62项通过（`tmp/ui-polish-footer-tests.log`）。另有109项聚焦通过；check通过、lint 0错误42警告、diff检查通过。
- 两名评审独立检查的5张390图无剩余可见P0/P1/P2，共同结论一致，无拒绝项。launch/ranking/profile：`tmp/ui-runs/simulator-frame/2026-09-12T15-57-41-390Z-73224-93966769/candidate/`；修后waterV2Member24Game/settings：`tmp/ui-runs/simulator-frame/2026-09-12T16-00-17-501Z-39368-f4a4069f/candidate/`。
- 上述为当前源码真实候选图的视觉复核：两批其他case失败，均未整批晋升，不宣称整批机器通过。曾有无样式/旧像素与页面状态不一致，已增加5case布局样式约束；旧像素问题仍可能发生，未声称根治工具问题。单独重建项目调试窗口后取得正常打水图。
- 未完成320/430、长战绩、完整长名展开、搜索空态、字体放大、键盘态及全15页最终视觉验收；因此这是首轮排版修复，不是全面UI验收完成。用户授权完成后自动真机调试，当前进入手机验收交付；没有提交推送、正式upload/发布或云部署。
- 已调用官方 `automation_viewport_action(remote, auto:true)` 启动自动真机调试；调用及随后只读systemInfo均超时，未重复启动，也未确认手机连接成功。后续先核对手机端状态，避免把工具超时当作成功或重复推送。

## 首页筛选与全局同类层级（用户真机截图追加）

- 用户指出首页排序/状态筛选大圆胶囊堆叠、分级不明确，授权全局优化同类问题；继续沿用不做 Edge 对比、最终自动真机调试的选择。
- 首页状态优先，四个等宽下划线标签，数量小号弱化；排序在下一行用轻量文字与真实选中勾。三种排序与四种筛选仍直接点击，不改筛选/排序业务。
- 同类修复覆盖偏好三组选择、两处A/B选队、场次数快捷项、录分快捷比分和打水二级流水筛选。中性未选、小圆角，选中使用明确边框/底色与真实勾号；保留44px触达。小型只读标签和已合理的选择器不机械改造。
- 新增homeToolbar的20赛事/5进行中/11草稿/4结束实图状态，以及shareSquadChoice选队状态；均为隔离截图数据，不是线上数据验证。
- 首页、偏好、分享选队、录分、打水流水五项390px原生图单批通过并晋升：`tmp/hierarchy-ui/runs390/simulator-frame/2026-09-12T16-16-20-515Z-18984-15ee70c6/manifest.json`。主控亲眼检查后补修流水正文被微信默认按钮宽度挤窄的问题，最终流水单项通过：`tmp/ui-runs/simulator-frame/2026-09-12T16-19-59-811Z-42424-6e6b2acf/manifest.json`；最终图 `tmp/hierarchy-ui/feed-final/simulator-frame/waterV2VisitorLong.png`。
- 主控及两名隔离评审均已查看前四图及修后流水图，无剩余可见P0/P1/P2。结论仅覆盖上述390px静态状态；320/430、大字体、完整交互及用户人工验收未完成，不宣称全尺寸或全15页验收通过。
- 首次全量1491项中1484通过、1失败、6跳过，失败为新增截图样式门禁对应的旧测试模拟对象缺少style方法；补齐后最终全量1485通过、0失败、6跳过（`tmp/hierarchy-full-final.log`），截图工具直接54/54通过（`tmp/hierarchy-regression-repair.log`）。本轮聚焦43/43通过；lint 0错误42警告，diff检查通过。
- 已为本次最终代码再次发起一次官方 `remote, auto:true` 自动真机调试，返回等待automator响应超时。未重复发送；手机自动打开及连接状态尚未确认。未提交、推送、正式upload/发布或部署云函数。

## 版本交付（2026-09-13）

- 用户随后明确授权“提交推送、上传该版本小程序，做好文档记录”；本轮提交整个已说明的UI修复范围，并上传开发版本。上传不等于提审或正式发布。
- 上传源为当前Git工作区 `D:/projects(WIN)/badminton-miniapp`，`miniprogramRoot=miniprogram/`；版本按 `6.1.2-<源码提交短号>` 生成。不采用preview镜像，不收录 `.playwright-cli/`、`preview-qrcodes/` 和 `tmp/` 运行产物。
- 交付前独立只读差异复核无阻断；原筛选、排序、选队动作参数保持，长名阈值变更有直接覆盖。复用最终全量与lint结果；补做check通过（`tmp/ui-upload-check.log`）。
- `mp:quality`执行成功，分析535文件、11项中8项通过、3项优化指标未满足：主包分析体积6524702字节超过1.5M建议值、未启用lazyCodeLoading、图片/音频资源超过200K（`tmp/delivery-quality/1789230702315.json`）。这不是质量全项通过，实际压缩上传包以官方上传回执为准。本轮未扩大为分包/加载机制改造。
