# Parallel Development Evidence Compatibility Area

> 状态：历史并行路线已暂停并归档。这里保留任务文档与机器 evidence 的原路径，是因为现有审计脚本仍直接读写 `docs/tasks/parallel-development/evidence`。

本目录不是当前任务入口。总路线图已移至 `docs/archive/2026/plans/parallel-development-roadmap.md`。

在迁移审计脚本、测试和证据 manifest 之前，不要机械移动或删除本目录。需要重新启动任何工作线时，必须基于当前 `AGENTS.md`、`docs/status/project-state.md` 和用户新授权重新规划。
