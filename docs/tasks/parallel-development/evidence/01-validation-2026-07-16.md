# 工作线 01 验证记录（2026-07-16）

## 环境与范围

- worktree：`D:\projects(WIN)\badminton-miniapp-worktrees\data-baseline`
- branch：`codex/roadmap-data-baseline`
- 工作线起始 HEAD：`70845c1`
- 数据拉取续作起始 HEAD：`0c79d1d`
- We 安全配置：worktree `.env.local=false`、token cache=false；只在进程内读取主工作区既有本地配置，未打印、复制或写日志，token 未持久化。
- DevTools session：project path、进程身份、CLI `39421`、automation `39420` 与 runtime binding 均验证绑定当前 worktree。
- 拉取完成后使用同一 session identity 退出；`39420` / `39421` 残留监听数为 0。
- 禁止范围：`miniprogram/**`、`cloudfunctions/**`、`docs/tasks/current.md`、总路线图、`AGENTS.md`、`package.json`、`package-lock.json` 均无任务改动。
- `project.config.json` 收口前工作区仅为 DevTools 行尾变化：JSON 语义相同、`git diff --ignore-space-at-eol` clean。核验工作区 SHA-256 `2d7ad2a853e0728748d51a85b0130476e58ff523c6d8220b01a062d285b20b8d` 后，只把该文件精确恢复为 HEAD 的 1380 bytes / SHA-256 `10b504863fed37bfeef481cddf97706b97885118ab62f41897e449dd37cfdae2`；未使用 reset/clean，也未覆盖其他改动。
- 受保护的 `tests/data-baseline-analysis.test.js` SHA-256 保持 `05bf98cb42b1dbdb8038a59bf5b86b7269a23ea063357e61f48d38b61e75d986`。逐项对比确认 7 项均为互补覆盖，单文件 7/7 pass，原样纳入测试体系与本地提交。

## We 分析只读拉取

| 验证 | 结果 |
|---|---|
| 截止日 / 请求范围 | `2026-07-15` / `2026-01-17..2026-07-15` |
| datacube 数据请求 | 931 / 931 success，0 fail |
| 传输 / 语义 | 官方协议要求 HTTPS POST；全部为只读查询，mutation/remote write 为 0 |
| token | 仅驻留内存，`tokenPersisted=false`，worktree cache 不存在 |
| dailySummary / dailyVisitTrend | 各 180 请求；153 日有 row、27 日 successful-empty；90 天为 90/90 有 row |
| visitPage / visitDistribution | 各 180 请求；129 日有语义数据、51 日 successful-empty；90 天为 90/90 |
| retention | 180 daily + 25 weekly + 5 monthly；只纳入实际存在的精确 offset |
| portrait | `2026-07-15` 单日；省市删除、k=5 小桶抑制 |
| 原始文件隔离 | 931 response JSON + 1 manifest + 818 CSV 位于 ignored `data/we-analysis/**`，tracked=0 |
| matrix / manifest contract | 8 source、931 精确作业、cutoff/start、results↔artifacts、JSON/CSV mapping、rowCount 与 read-only contract 全部为 true |
| 确定性脱敏摘要 | 双跑字节一致，SHA-256 `1d84ea5376f9295a7fc016f212a0b3e78412e760ac0e1ede351a980034dcc090` |
| 脱敏语义 | route 去 query/hash；UV 标为 person-days；成功空响应不补 0；画像不含地理与小样本值 |

## 只读数据库快照

| 验证 | 结果 |
|---|---|
| session project / process / runtime binding | 全部匹配当前 worktree |
| `tournaments` count before / exported / after | 1070 / 1070 / 1070 |
| keyset page size | 20，`_id` 严格递增，无重复/缺失 ID |
| 连续快照 | 2 遍，均 1070 条 |
| 连续规范化 SHA-256 | `e3bb1aed23e3d00a8545b3baea04550a7b1de04420b85ccdf180a3532605b65b` |
| `client_request_logs` 客户端可见数 | 0，连续 2 遍一致；管理员可见性未验证，不判定服务端为空 |
| 原始文件隔离 | `data/we-analysis/**` 命中 `.gitignore`；`git ls-files -- data/we-analysis .cache .env.local` 无输出 |
| 云端写操作 | 0 |

## 分析确定性与抽样

- 90 天纳入 944 条、已开赛 482 条、严格有效完赛 165 条。
- 180 天纳入 1065 条、已开赛 547 条、严格有效完赛 190 条。
- 真实输入重新运行后，90/180 天各 6 个产物（共 12 个）SHA-256 全部与首遍一致。
- 离线分层抽样 30 条：draft/running/finished 各 10；status、独立比分形状复核和公开维度字符约束均 0 mismatch。
- 输入守恒、status 守恒均为 true；180 天 started 核心组合分类率 97.6%。
- 180 天 Pareto JSON 与本地聚合源哈希一致；CSV 可解析 182 行。

## 测试与检查

| 验证 | 结果 |
|---|---|
| `node --test`：fetch + baseline + We core/batch + exporter/CLI | 59 / 59 pass |
| Windows PowerShell 解析只读 exporter | pass |
| `npm test`（由 `npm run verify:full` 执行） | 1214 pass / 0 fail / 6 skip（1220 total） |
| `npm run check`（由 `npm run verify:full` 执行） | deprecated wx API 与 9 templates / 22 cloud functions shared-lib 检查通过 |
| `npm run lint`（由 `npm run verify:full` 执行） | exit 0；0 errors，64 个既有 warning，触及文件无新增 warning |
| 6 个 `01-*.json` | 全部 `ConvertFrom-Json` 成功 |
| Pareto CSV | `Import-Csv` 成功，182 行 |
| 真实输入双次确定性 | We 两次与 tracked summary 字节一致；赛事 90/180 天各 6 文件，两次 12 / 12 哈希一致 |
| 公开摘要与本地聚合交叉一致性 | 33 / 33 checks pass |
| 原始身份/资料 token 对公开文件反向扫描 | 8841 个源 token × 10 个公开任务/证据文件，0 命中 |
| 公开 JSON 身份字段、地理画像与敏感 route 扫描 | forbidden key / geo value / query-fragment route / k<5 bucket 均 0；重叠 cohort 差分保护=true |
| 真实本地凭据精确值对公开/候选文件扫描 | 3 个仅内存值 × 16 个候选文件，0 命中；未输出值 |
| raw/cache/tmp/secret 路径 tracked 文件 | 0 |
| `npm run verify:full` | exit 0（test + check + lint + `git diff --check`） |
| 暂存范围 / 禁止路径 / secret 形状 | 16 files / 0 / 0；真实本地凭据精确值 0 命中 |
| `git diff --cached --check` | pass |

先前执行 `npm ci` 安装 lockfile 依赖时报告现有依赖树含 78 个 audit 告警（1 low / 18 moderate / 14 high / 45 critical）；本工作线未执行 `npm audit fix`，避免越界升级依赖。

## 隐私与解释边界

- 原始赛事快照只位于 ignored `data/we-analysis/**`，未复制赛事 ID、名单、昵称、头像、手机号、位置、赛事名或原始行到公开证据。
- Pareto 只含 allowlist mode/preset/template/engine、人数、场地、场数与聚合计数；最小样本组合不含用户维度。
- 公开 JSON 不含 actor 标识；来源 manifest 只记录计数、时间范围和 SHA-256。
- We 180 个日请求完整，但 27 个 early daily trend 空响应按 unavailable 披露；留存缺失 offset 排除，均未用 0 替代。
- We route 只保留 query/hash 之前的标准路径；画像仅允许 ages/genders/platforms/devices 的 k>=5 聚合，province/city 和小桶值不进入公开证据。
- UV、新 UV、share UV 使用 person-days 口径，累计用户仅报告端点值与差，均不冒充窗口去重用户。
- 客户端可见 1070 条不能证明管理员全量；`client_request_logs=0` 不能证明服务端集合为空。
- `shareActivity*` 不作为实际分享证据；28 日用户指标标记为 snapshot/survivor-based proxy。

## 未执行操作

未执行真实云数据写入、集合创建、云函数部署、preview/upload、正式发布、push、PR 或 merge。
