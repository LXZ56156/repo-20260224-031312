# 工作线 01 验证记录（2026-07-16）

## 环境与范围

- worktree：`D:\projects(WIN)\badminton-miniapp-worktrees\data-baseline`
- branch：`codex/roadmap-data-baseline`
- 起始 HEAD：`70845c1`
- 初始 `git status`：clean
- 凭据/缓存存在性检查：`.env.local=false`、`.cache=false`、`data/we-analysis=false`；仅检查存在性，未打印 secret。
- 微信开发者工具：使用当前 worktree 启动，CLI `39421` 可用；自动化 `39420` 超时未就绪，未执行数据库查询。
- 禁止范围审计：`miniprogram/**`、`cloudfunctions/**`、`docs/tasks/current.md`、总路线图、`AGENTS.md`、`package.json`、`package-lock.json` 均无改动。

## 测试与检查

| 验证 | 结果 |
|---|---|
| `node --test tests/data-baseline-core.test.js tests/data-baseline-cli.test.js` | 24 / 24 pass |
| 加上外部未跟踪契约测试 `tests/data-baseline-analysis.test.js` | 31 / 31 pass |
| CLI 对合成导出连续运行两次 | 6 个输出文件 SHA-256 完全一致；`DETERMINISTIC=True` |
| CLI 合成身份 token 扫描 | `PII_TOKEN_PRESENT=False` |
| `npm test`（安装 lockfile 依赖后） | 1190 pass / 0 fail / 6 skip |
| `npm run check` | deprecated wx API 与 9 templates / 22 cloud functions shared-lib 检查通过 |
| `npm run lint` | exit 0；0 errors，64 个既有 warning |
| 5 个新增 JSON 产物/fixture 解析 | 全部 `ConvertFrom-Json` 成功 |
| secret-shaped 扫描 | 0 matches |
| 禁止生产路径改动扫描 | 0 matches |
| `git diff --cached --check` | pass |

首次全量测试在缺少 `node_modules/canvas` 时出现 4 个模块加载失败；执行 `npm ci` 后原命令完整通过。`npm ci` 只生成忽略的 `node_modules/`，未改 package manifest 或 lockfile。安装过程报告锁文件现有依赖树含 78 个 audit 告警（1 low / 18 moderate / 14 high / 45 critical）；本工作线未执行 `npm audit fix`，避免越界升级依赖。

## 确定性、守恒与隐私

- 重复快照按 `_id` 保留最新 `updatedAt`，并验证 input = invalid + duplicate discarded + deduplicated。
- status 分类、计划/物化场数、合法比分数、时间戳覆盖、逐维 missing/unknown 均进入质量摘要。
- `scoredAt` 不完整时不计算完赛时间或耗时；旧 proxy 来源单独标记。
- 管理员导入 guest 不进入真实参与者 cohort；公开摘要不输出 actor ID、昵称、头像、手机号、位置或赛事名。
- scheduler 维度采用业务 allowlist/精确格式；CLI 在写文件前扫描源身份/资料 token，命中即拒绝写出。
- 原始缓存与本地分析继续位于 `.gitignore` 范围；提交证据只有聚合口径和 source-unavailable 状态。

## 未执行操作

未执行真实云数据写入、集合创建、云函数部署、preview/upload、正式发布、push、PR 或 merge。
