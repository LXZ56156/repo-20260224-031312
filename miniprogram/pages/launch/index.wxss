/* ============================================================
   Launch 页面 — 赛制选择面板
   ============================================================ */

.launch-page {
  padding-bottom: calc(env(safe-area-inset-bottom) + 24rpx);
}

/* --- Hero --- */
.launch-hero {
  padding: var(--space-hero-pad-y) var(--space-hero-pad-x);
  border-radius: 24rpx;
  background:
    radial-gradient(circle at 88% 12%, rgba(24, 179, 104, 0.16) 0%, rgba(24, 179, 104, 0.04) 30%, transparent 56%),
    linear-gradient(180deg, rgba(243, 252, 246, 0.99) 0%, rgba(255, 255, 255, 0.99) 58%, #ffffff 100%);
  color: var(--neutral-950);
  border: 1rpx solid rgba(24, 179, 104, 0.12);
  box-shadow: var(--shadow-card);
}

.launch-hero-title {
  font-size: 40rpx;
  font-weight: 800;
  line-height: 1.2;
  letter-spacing: 0;
}

.launch-hero-sub {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: var(--neutral-500);
  line-height: 1.45;
}

/* --- Mode Card --- */
.launch-card {
  margin-top: var(--space-section);
  position: relative;
  overflow: hidden;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.99) 0%, rgba(248, 251, 249, 0.98) 100%);
}

.launch-water-card {
  border-color: rgba(39, 123, 214, 0.18);
  background: linear-gradient(135deg, #f4f9ff 0%, #ffffff 74%);
  box-shadow: 0 10rpx 28rpx rgba(39, 123, 214, 0.1);
}

.launch-water-accent {
  width: 8rpx;
  background: #277bd6;
}

.launch-water-kicker {
  margin-left: var(--space-tight);
  margin-bottom: 8rpx;
  color: var(--info);
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 2rpx;
}

.launch-water-btn {
  color: #ffffff;
  background: var(--info);
}

.launch-section-label {
  margin: 28rpx 4rpx 0;
  color: var(--neutral-700);
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 2rpx;
}

/* 左侧色彩暗示条 */
.launch-card-accent {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 6rpx;
  border-radius: 20rpx 0 0 20rpx;
  background: var(--neutral-300);
  transition: background 200ms ease;
}

/* 不同模式的色彩暗示 */
.launch-card-rotation_6 .launch-card-accent,
.launch-card-rotation_7 .launch-card-accent,
.launch-card-rotation_8 .launch-card-accent,
.launch-card-multi .launch-card-accent {
  background: var(--brand-500);
}

.launch-card-squad .launch-card-accent {
  background: var(--info-500);
}

.launch-card-fixed .launch-card-accent {
  background: #F59E0B;
}

/* 默认推荐模式 — 轻微强调 */
.launch-card.is-default {
  border-color: rgba(24, 179, 104, 0.18);
  box-shadow: 0 10rpx 24rpx rgba(24, 179, 104, 0.12);
}

.launch-card.is-default .launch-card-accent {
  width: 8rpx;
}

.launch-row {
  align-items: flex-start;
  gap: var(--space-inline);
  padding-left: var(--space-tight);
}

.launch-title {
  font-size: 32rpx;
  font-weight: 780;
  line-height: 1.3;
  color: var(--neutral-950);
}

.launch-sub {
  margin-top: 8rpx;
  line-height: 1.5;
  color: var(--neutral-500);
  font-size: 24rpx;
}

.launch-actions {
  margin-top: var(--space-section);
  padding-left: var(--space-tight);
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: var(--space-inline);
}

/* 规则说明 — text link 弱化 */
.launch-rule-link {
  min-height: 44px;
  display: inline-flex;
  align-items: center;
  padding: 0 8rpx;
  color: var(--neutral-500);
  font-size: 24rpx;
  font-weight: 500;
  text-decoration: underline;
  text-underline-offset: 4rpx;
}

.launch-action-spacer {
  visibility: hidden;
  pointer-events: none;
}

/* 发起按钮 — primary CTA */
.launch-btn {
  min-width: 160rpx;
}
