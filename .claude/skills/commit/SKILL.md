# Commit Skill
1. Run `git status` and `git diff --stat` to review all changes
2. Categorize changes by relevance and group logically
3. Run full test suite (`npm test`) and confirm all pass
4. Commit with descriptive Chinese commit message
5. Show summary of what was committed
